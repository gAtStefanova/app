import { AppImageUrlDirective } from './app-image-url.directive';

describe('AppImageUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new AppImageUrlDirective();
    expect(directive).toBeTruthy();
  });
});
